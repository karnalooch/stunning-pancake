appId: com.sport.athlete
---
# GPS recovery banner (optional) after authenticated launch.
- launchApp
- extendedWaitUntil:
    visible: "Ride"
    timeout: 60000
- assertVisible:
    text: ".*GPS.*"
    optional: true
