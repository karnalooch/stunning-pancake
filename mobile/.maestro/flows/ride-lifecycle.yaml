appId: com.sport.athlete
---
# Authenticated user reaches Ride tab (E2E auto-login baked into local APK).
- launchApp
- extendedWaitUntil:
    visible: "Ride"
    timeout: 60000
- tapOn: "Ride"
- assertVisible:
    text: ".*[Ss]tart.*"
    optional: true
