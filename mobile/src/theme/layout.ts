/**
 * Shared spacing contract for top-level screen layouts.
 * Keeps Ride / Explore / Profile surfaces visually aligned.
 */
export const LAYOUT = {
  gutter: 16,
  sectionGap: 16,
  compactGap: 8,
  tabBarBottomInset: 80,
} as const;
