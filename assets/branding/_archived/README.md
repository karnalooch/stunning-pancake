# _archived

This directory holds legacy branding assets that have been superseded by the `@tokens/generated/restyle-colors` design token system.

## Files

- **design_tokens.json** — Original Tamagui-era design tokens. Replaced by `@tokens/generated/restyle-colors` (generated from Unistyles theme definitions). Archived as of Phase 4 Tamagui Removal.

## Retention

These files are kept for historical reference only. They are not imported or used by any active code. Safe to remove after the next major release if no rollback is needed.
