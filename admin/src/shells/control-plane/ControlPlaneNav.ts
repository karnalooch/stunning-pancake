/**
 * GLOBAL_OWNER control-plane nav extensions.
 * Full shell split is incremental — this module holds GO-only route metadata.
 */
export const CONTROL_PLANE_ROUTES = {
  actionInbox: '/owner/control-plane/inbox',
} as const;
