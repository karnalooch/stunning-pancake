# ⚖️ SPORT PLATFORM CONSTITUTION (v2.1 Gold Master)
> "Sovereignty through Code. Performance through Discipline."

---

## 🏗️ PILLAR I: ARCHITECTURE & TECHNOLOGY (The Core Directives)

### 1.1 "Power Couple" Strategy
| Component | Technology | Objective |
| :--- | :--- | :--- |
| **Core (Backend)** | Python (Django/FastAPI) | Logical Speed & ML |
| **Interface (Front/Mobile)** | TypeScript (React 19 / RN 0.81) | Typing & UI Performance |
| **Design System** | Tamagui / Mantine v7 | Cyber-Monolith Consistency |
| **Orchestration** | Kubernetes (Kind/Podman) | Cloud-Native Parity |

*   **SaaS Isolation Principle:** Database (PostgreSQL/Citus) is an external service. SPORT code remains free of copyleft (GPL) infection.
*   **Owner Sovereignty:** A superior `Owner Panel` for managing the entire network of cities and corporations.

### 1.2 Performance & Quality
*   **60 FPS Mandate:** Telemetry and charts MUST run on GPU (Skia). Maximum UI thread time: **16ms**.
*   **Zero-Regression QA:** Bug fixes require a reproducing test BEFORE the patch is created.
*   **API Evolution:** Mandatory backward compatibility and Semantic Versioning (SemVer).

---

## 🤖 PILLAR II: AGENTIC WORKFLOW (AI-First Protocols)

### 2.1 Gemini CLI (Master Tool)
*   **Identity:** Alias `geminicli`, binary `gemini.cmd`.
*   **Path (Win):** `C:\Users\akarn\AppData\Roaming\npm\gemini.cmd`
*   **Usage:** Used for audits, code evolution, and report generation. Complex tasks delegated to the `gemini-3.1-pro-preview` model.

### 2.2 Model Hybrid Hygiene (Rule 14)
*   **Junior/Senior Split:** `Flash` (Scaffolding/UI), `Pro` (Logic/Optimization).
*   **Performance Markers:** Critical code (Legend-State) MUST have the `[PERFORMANCE CRITICAL]` comment to avoid incorrect refactoring by lighter models.
*   **Sanity Check:** Any Flash change in core logic requires `tsc` verification or a Pro-model Audit.

---

## 🔄 PILLAR III: PROJECT LIFECYCLE (The Milestone Loop)

Every Milestone MUST end with the following sequence:
1.  **Audit:** Run `gemini -p "Analyze state... and generate report"` (State analysis).
2.  **Documentation:** Entry in `docs/audits/code_and_docs_audit.md`.
3.  **Commit:** `git commit -m "milestone: ..."` (Clear version reference).
4.  **Push:** Synchronization with the remote repository.

---

## 🛡️ PILLAR IV: SECURITY & COMPLIANCE

*   **Privacy-by-Design:** Absolute priority on location privacy (Article 10) and PCI DSS.
*   **Compliance:** 100% compliance with GDPR and tax regulations (VAT OSS/JPK).
*   **Non-Root:** Containers MUST run as non-root (UID 1001/101).

---

## 🎨 "CYBER-MONOLITH" AESTHETICS

*   **Colors:** Primary: **Cyan (#00D1FF)**, Accent: **Purple (#B066FF)**.
*   **Style:** Mica/Fluent, Glassmorphism, Spring Animations.

---

## 🚨 DISASTER RECOVERY (Rescue Plan)
1. **Bootstrap:** Run `.\setup-environment.ps1` (Tool installation).
2. **Cluster:** `kind create cluster --name kind-cluster`.
3. **Start:** `.\dev.ps1` (Automatic world rebuild).

---

## 🏁 MILESTONE TRACKER
- [x] **v2.1 Gold Master (K8s & Security):** Full orchestration + Zero-vulnerability audit.
- [x] **Enterprise Quality Standards:** 60 FPS Mandate + Zero-Regression QA.
- [x] **Phase 4 Mobile Upgrade:** Migration to Tamagui + Legend-State.
- [ ] **Global Rollout:** First city instance in the cluster. (In Progress)
